import {
  // create naive ui
  create,
  // component
  NButton,
  NImage,
  NSpace,
  NDropdown,
  NIcon,
  NMenu,
  NCarousel
} from 'naive-ui'

export const naive = create({
  components: [NButton, NImage, NSpace, NDropdown, NIcon, NMenu, NCarousel]
})
